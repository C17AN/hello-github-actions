name: "Hello Actions"
description: "Greet Someone"
author: "c17an@github.com"

inputs:
  MY_NAME:
    description: "Who to Greet"
    required: true
    default: "World"
    
runs:
  using: "docker"
  image: "Dockerfile"
 
branding:
  icon: "mic"
  color: "purple"
